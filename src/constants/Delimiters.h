#pragma once

#include <string>

using namespace std;

namespace Delimiters {
    const string ACCOUNT_FIELDS = "*0-#-0*";
    const string BALANCES_FIELDS = "*1-#-1*";
    const string BALANCE_FIELDS = "*=*";
    const string PERMISSIONS = "*2-#-2*";
}